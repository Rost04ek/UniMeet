# 🎓 Система організації студентських подій і конференцій

## 📋 Опис проекту

Веб-додаток для організації та управління студентськими подіями, конференціями та семінарами. Система дозволяє студентам знаходити цікаві події, реєструватися на них, а організаторам - створювати та керувати подіями.

### Автор
**Курсова робота**  
Виконана на: Python + Flask + MySQL

## ✨ Основні функції

### Для студентів:
- ✅ Реєстрація та авторизація в системі
- 📅 Перегляд майбутніх подій
- 🔍 Фільтрація подій за категоріями та статусом
- 📝 Реєстрація на події
- 💬 Коментування подій
- ⭐ Оцінювання подій
- 👤 Особистий профіль з історією реєстрацій

### Для організаторів:
- ➕ Створення нових подій
- ✏️ Редагування та управління подіями
- 👥 Перегляд списку учасників
- 📊 Відстеження кількості реєстрацій
- 📈 Статус подій (майбутня, поточна, завершена)

## 🛠️ Технології

- **Backend**: Python 3.x, Flask 3.0
- **Frontend**: HTML5, CSS3, JavaScript
- **База даних**: MySQL 8.0
- **Безпека**: Werkzeug (хешування паролів)
- **Конфігурація**: python-dotenv

## 📁 Структура проекту

```
KYS/
├── app.py                  # Головний файл додатку (Flask)
├── config.py               # Конфігурація підключення до БД та Flask
├── models.py               # Робота з базою даних (MySQL)
├── requirements.txt        # Залежності Python
├── start.bat               # Windows-скрипт швидкого запуску
├── database/
│   ├── schema.sql          # SQL-схема бази даних
│   └── universities.sql    # Довідник університетів (опційно)
├── templates/              # HTML-шаблони (Jinja2)
│   ├── base.html
│   ├── index.html
│   ├── login.html
│   ├── register.html
│   ├── events.html
│   ├── event_detail.html
│   ├── create_event.html
│   ├── edit_event.html
│   ├── my_events.html
│   ├── profile.html
│   ├── participants.html
│   ├── users.html
│   └── edit_user.html
├── static/
│   ├── css/
│   │   └── style.css       # Стилі
│   ├── js/
│   │   └── main.js         # Клієнтський JavaScript
│   └── images/             # Зображення (логотип)
└── README/README.md        # Цей файл
```

## 🚀 Встановлення та запуск

### Передумови
- Python 3.8 або новіша версія
- MySQL 8.0 або новіша версія
- pip (менеджер пакетів Python)

### Крок 1: Клонування репозиторію
```bash
cd UniMeet
```

### Крок 2: Створення віртуального середовища
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### Крок 3: Встановлення залежностей
```bash
pip install -r requirements.txt
```

### Крок 4: Налаштування бази даних

1. **Створіть базу даних MySQL:**
```sql
CREATE DATABASE student_events_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

2. **Імпортуйте схему:**
```bash
mysql -u root -p student_events_db < database/schema.sql
```

 3. **(Опційно) Імпортуйте довідник університетів:**
 ```bash
 mysql -u root -p student_events_db < database/universities.sql
 ```

### Крок 5: Налаштування змінних середовища

Створіть файл `.env` у корені проекту та додайте ваші налаштування (якщо додаток використовує змінні середовища):
```env
MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=ваш_пароль
MYSQL_DB=student_events_db
MYSQL_PORT=3306

SECRET_KEY=ваш_секретний_ключ
DEBUG=True
```

### Крок 6: Запуск додатку
На Windows можна скористатися скриптом швидкого запуску:
```bash
start.bat
```

Або запустіть вручну:
```bash
python app.py
```

Додаток за замовчуванням доступний за адресою: `http://localhost:5000`

## 📊 Структура бази даних

### Таблиці:

1. **users** - Користувачі системи
   - id, email, password_hash
   - full_name, role, university, faculty
   - created_at

2. **event_categories** - Категорії подій
   - id, name, description
   - created_at

3. **events** - Події
   - id, title, description, category_id
   - organizer_id, location, event_date
   - registration_deadline, max_participants
   - current_participants, status, image_url
   - created_at, updated_at

4. **registrations** - Реєстрації на події
   - id, event_id, user_id
   - registration_date, status, notes

5. **comments** - Коментарі до подій
   - id, event_id, user_id
   - comment_text, created_at

6. **ratings** - Оцінки подій
   - id, event_id, user_id
   - rating, review, created_at

## 👥 Ролі користувачів

1. **Студент (student)** - базова роль
   - Перегляд подій
   - Реєстрація на події
   - Коментування та оцінювання

2. **Організатор (organizer)**
   - Всі можливості студента
   - Створення подій
   - Управління своїми подіями
   - Перегляд учасників

3. **Адміністратор (admin)**
   - Повний доступ до системи
   - Управління всіма подіями
   - Управління користувачами

## 🎨 Особливості інтерфейсу

- ✅ Адаптивний дизайн (responsive design)
- 🎯 Інтуїтивна навігація
- 💫 Анімації та плавні переходи
- 📱 Підтримка мобільних пристроїв
- 🌈 Сучасний Material Design

## 🔒 Безпека

- 🔐 Хешування паролів (Werkzeug)
- 🛡️ Захист від SQL ін'єкцій
- 🔑 Сесійна автентифікація
- ✅ Валідація форм на клієнті та сервері
 - 🔒 Зберігання секретів через `.env` (рекомендовано)

## 📝 API Endpoints

### Публічні маршрути:
- `GET /` - Головна сторінка
- `GET /register` - Форма реєстрації
- `POST /register` - Реєстрація користувача
- `GET /login` - Форма входу
- `POST /login` - Вхід користувача
- `GET /events` - Список подій
- `GET /event/<id>` - Деталі події

### Захищені маршрути (потрібна авторизація):
- `GET /logout` - Вихід
- `GET /profile` - Профіль користувача
- `POST /event/<id>/register` - Реєстрація на подію
- `POST /event/<id>/cancel` - Скасування реєстрації
- `POST /event/<id>/comment` - Додавання коментаря

### Маршрути організатора:
- `GET /create-event` - Форма створення події
- `POST /create-event` - Створення події
- `GET /my-events` - Мої події
- `GET /event/<id>/edit` - Редагування події
- `POST /event/<id>/edit` - Збереження змін
- `GET /event/<id>/participants` - Список учасників

## 📈 Можливості розширення

- 📧 Email-сповіщення про події
- 🔔 Push-повідомлення
- 📊 Статистика та аналітика
- 🖼️ Завантаження зображень подій
- 📅 Інтеграція з календарями
- 🔍 Розширений пошук та фільтрація
- 💳 Платні події (інтеграція з платіжними системами)
- 📱 Мобільний додаток
- 🌍 Мультимовність
- 📹 Онлайн трансляції подій

## 🐛 Відомі проблеми

На даний момент немає критичних помилок. Якщо ви знайшли баг, будь ласка, створіть issue.

## 📄 Ліцензія

Цей проект створено як курсова робота. Використовуйте вільно для навчальних цілей.

## 👨‍💻 Контакти

Якщо у вас є питання або пропозиції, зв'яжіться зі мною.
